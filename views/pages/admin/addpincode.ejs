<!DOCTYPE html>
<html lang="en">

<head>
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
        }

        .sidebar {
            width: 250px;
            height: 100vh;
            background: #343a40;
            padding-top: 20px;
            position: fixed;
            left: 0;
            top: 0;
        }

        .sidebar a {
            display: block;
            color: white;
            padding: 15px;
            text-decoration: none;
        }

        .sidebar a:hover {
            background: #495057;
        }

        .main-content {
            margin-left: 250px;
            padding: 20px;
        }

        .card {
            margin-bottom: 20px;
        }

        /* Popup message */
        .popup {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: white;
            padding: 20px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            border-radius: 10px;
            text-align: center;
            z-index: 1000;
        }

        .popup button {
            background-color: teal;
            color: white;
            border: none;
            padding: 10px 15px;
            cursor: pointer;
            margin-top: 10px;
        }

        .popup button:hover {
            background-color: darkslategray;
        }
    </style>
</head>

<body>
    <div class="sidebar">
        <h4 class="text-white text-center">Admin Panel</h4>
        <a href="/admin/1111"><i class="fas fa-users"></i> Dashboard</a>
        <a href="/admin/users/1111"><i class="fas fa-users"></i> Users</a>
        <a href="/admin/medicines/1111"><i class="fas fa-pills"></i> Medicines</a>
        <a href="/admin/products/1111"><i class="fas fa-box"></i> Products</a>
        <a href="/admin/orders/1111"><i class="fas fa-shopping-cart"></i> Orders</a>
        <a href="/admin/pincodes/1111"><i class="fas fa-map-marker-alt"></i> Pincodes</a>
        <a href="/admin/reports/1111"><i class="fas fa-chart-line"></i> Reports</a>
        <a href="/admin/settings/1111"><i class="fas fa-cog"></i> Settings</a>
        <a href="/users/logout" class="text-danger"><i class="fas fa-sign-out-alt"></i> Logout</a>
    </div>

    <!-- Error Message Popup -->
    <div id="error-popup" class="popup" style="display: none;">
        <p id="popup-message"></p>
        <button onclick="closePopup()">OK</button>
    </div>

    <div class="main-content">
        <h2>Add Pincode</h2>

        <form action="/admin/add-pincode/1111" method="POST" class="mt-3">
            <div class="form-group">
                <label>Pincode:</label>
                <input type="text" name="pincode" class="form-control" required>
            </div>

            <button type="submit" class="btn btn-primary" style="margin-top: 20px;">Add Pincode</button>
        </form>

    </div>


</body>

</html>

<script>

    document.addEventListener("DOMContentLoaded", function () {
        function showPopup(message, bgColor = "grey", textColor = "white") {
            const popup = document.createElement("div");
            popup.innerText = message;
            popup.style.position = "fixed";
            popup.style.top = "0";
            popup.style.width = "100%";
            popup.style.left = "0";
            popup.style.padding = "10px 20px";
            popup.style.textAlign = "center";
            popup.style.backgroundColor = bgColor;
            popup.style.color = textColor;
            popup.style.zIndex = "1000";

            document.body.appendChild(popup);

            setTimeout(() => {
                popup.remove();
                window.location.href = "/admin/add-medicines/1111";
            }, 3000);
        }

        function closePopup() {
            document.getElementById("error-popup").style.display = "none";
            const url = new URL(window.location);
            url.searchParams.delete("error");
            window.history.replaceState({}, document.title, url);
        }

        document.querySelector("#error-popup button").addEventListener("click", closePopup);

        // 🔹 Check for error message in URL
        const urlParams = new URLSearchParams(window.location.search);
        const errorMessage = urlParams.get("error");
        if (errorMessage) {
            showPopup(decodeURIComponent(errorMessage), "red", "white");
        }
    });
</script>